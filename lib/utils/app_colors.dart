import 'package:flutter/material.dart';

/// Palette de couleurs de l'application
class AppColors {
  // Fond écran principal
  static const Color primaryBackground = Color(0xFF252d3c);
  
  // Fond écran menus (AppBar, etc.)
  static const Color menuBackground = Color(0xFF2e3749);
  
  // Fond écran et écritures secondaires
  static const Color secondaryBackground = Color(0xFF3f8deb);
  static const Color secondaryText = Color(0xFF3f8deb);
  
  // Boutons/textes importants
  static const Color primaryButton = Color(0xFF39aeea);
  static const Color primaryText = Color(0xFF39aeea);
  
  // Couleurs pour les textes et éléments UI
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFF3f8deb);
  static const Color textDisabled = Colors.white60;
  
  // Couleurs pour les cartes et conteneurs
  static const Color cardBackground = Color(0xFF2e3749);
  
  // Couleurs pour les icônes
  static const Color iconPrimary = Color(0xFF39aeea);
  static const Color iconSecondary = Color(0xFF3f8deb);
  static const Color iconDisabled = Colors.white60;
}

